#####
# This is the fancy sxhkd config file. Start with program hotkeys.
#####

# Make sxhkd reload its configuration.
alt + shift + Escape
    pkill -USR1 -x sxhkd

# Terminal
alt + {_,shift +} Return
    urxvt256c

# rofi
alt + {_,shift +} r
    rofi -show run

# Google Chrome
alt + {_,shift +} f
    google-chrome

# Lockscreen
alt + {_,shift +} l
    lock

#####
# BSPWM Hotkeys
#####

# Exit BSPWM
alt + shift + q
    bspc quit

# Close/kill a window.
alt + shift + {_,super +} c
    bspc node -{c,k}

# Switch to another desktop.
alt + {1-9,10}
    bspc desktop -f ^{1-9,10}

# Move a node to another desktop.
alt + shift + {1-9,0}
    bspc node -d ^{1-9,10}

# Swap the focused node with the biggest node.
alt + b
    bspc node -s biggest.local

# Set a window as either floating or tiled.
alt + {_,shift +} space
    bspc node -t {tiled,floating}

# Select or swap nodes according to direction.
alt + {_,shift +} {h,j,k,l,Left,Right,Up,Down}
    bspc node -{f,s} {west,south,north,east,west,east,north,south}

# Rotate through local windows.
alt + Tab
    bspc node -f next.local

# Select parent node.
alt + {_,shift +} p
    bspc node -f parent
